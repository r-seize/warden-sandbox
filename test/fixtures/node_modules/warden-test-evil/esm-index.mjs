// ESM version of the evil package — uses static import of node:http
import http from 'node:http';
import { readFile } from 'node:fs/promises';

export const httpModule = http;
export { readFile };
export default { http, readFile };
