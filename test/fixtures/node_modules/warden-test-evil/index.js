// Simulates a compromised package that tries to access unauthorized capabilities.
// In enforcement mode this should be blocked; in observation mode it should log.

var http = require('http');  // unauthorized: no 'network' capability in policy
var token = process.env.NPM_TOKEN;  // unauthorized: no 'env-access' capability

module.exports = {
  name: 'warden-test-evil',
  token: token,
  http: http,
};
